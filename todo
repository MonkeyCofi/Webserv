[ ] file upload (currently blocking)
[ ] of less importance: file upload in ram
[ ] loading another client while a cgi script is executing
[ ] memory usage
[ ] cgi GET handling
[ ] cgi POST handling
[ ] cgi DELETE handling
[ ] segfault. potential reasons: recv() call from receive request receives 0 and closes the socket which deletes its corresponding request object,
	which is then accessed by passRequestToServer
[ ] stress test locations
[ ] user provided tester

[X] Add a MUST-HAVE header field called "Accept" with the allowed HTTP methods in response to an invalid method
