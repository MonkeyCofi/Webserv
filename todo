[X] file upload (currently blocking)
[X] loading another client while a cgi script is executing
[-] memory usage
[X] cgi GET handling
[X] cgi POST handling
[X] cgi DELETE handling
[X] segfault. potential reasons: recv() call from receive request receives 0 and closes the socket which deletes its corresponding request object,
	which is then accessed by passRequestToServer
[X] stress test locations
[ ] user provided tester

[X] Add a MUST-HAVE header field called "Accept" with the allowed HTTP methods in response to an invalid method
